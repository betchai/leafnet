{
  "_comment": "Centralized training configuration. Methodology reference values (224x224, Adam, lr 1e-4) are starting points; every experiment records its actual values. Never scatter these through code.",
  "experimentDefaults": {
    "imageSize": [224, 224],
    "batchSize": 8,
    "learningRateHead": 0.0001,
    "learningRateBackbone": 0.00001,
    "epochs": 20,
    "optimizer": "adam",
    "weightDecay": 0.0001,
    "scheduler": "reduce_lr_on_plateau",
    "schedulerFactor": 0.5,
    "schedulerPatience": 2,
    "earlyStopping": true,
    "patience": 5,
    "numWorkers": 0,
    "randomSeed": 42,
    "dropout": 0.5,
    "freezeBackbone": true,
    "fineTuneLayers": 0
  },
  "augmentation": {
    "_rationales": {
      "horizontalFlip": "Leaf disease symptoms are left-right symmetric in nature; flipping doubles effective data without altering symptom appearance.",
      "rotation10": "Handheld photos arrive at arbitrary orientations; small rotations mimic real capture variance.",
      "colorJitter": "Mild brightness/contrast variation mimics lighting differences between sessions; kept mild to avoid distorting lesion colors that define the classes."
    },
    "horizontalFlip": 0.5,
    "verticalFlip": 0,
    "rotationDegrees": 10,
    "brightness": 0.15,
    "contrast": 0.15,
    "saturation": 0.05
  },
  "preprocessing": {
    "resize": [224, 224],
    "interpolation": "bilinear",
    "normalization": {
      "mean": [0.485, 0.456, 0.406],
      "std": [0.229, 0.224, 0.225],
      "_note": "ImageNet statistics required by torchvision MobileNetV2 pretrained weights"
    }
  },
  "classMappingSource": "../config/classes.json"
}
